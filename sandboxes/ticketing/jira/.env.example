# Copy to .env and adjust if you want non-default credentials.
# .env is gitignored.
#
# Set after you complete the one-time setup wizard at http://localhost:8080.

JIRA_USER=admin
JIRA_PASSWORD=admin
JIRA_URL=http://localhost:8080
JIRA_PROJECT_KEY=LSB
JIRA_PROJECT_NAME=Lets Sandbox
