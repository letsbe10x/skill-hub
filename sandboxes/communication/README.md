# Communication sandboxes

Local environments for chatops and team messaging tools. None landed yet — see
[../README.md](../README.md) for the sandbox contract.

Planned (in priority order):

| Tool | Self-host story |
|---|---|
| `mattermost/` | Fully self-hostable, official Docker image (`mattermost/mattermost-team-edition:latest`) |
| `rocketchat/` | Self-hostable (`rocketchat/rocket.chat:latest`) |
| `slack/` | No self-host — recorded HTTP fixtures only |
| `discord/` | No self-host — recorded fixtures only |
| `ms-teams/` | No self-host — recorded fixtures only |
