# Service context — {{repo_name}}
# Generated by lets-bootstrap-repo v2.0
# Owner should review and commit this file

service:
  purpose: "{{purpose}}"
  type: "{{type}}"
  approved_by: "{{approved_by}}"

  non_negotiables:
    {{#non_negotiables}}
    - "{{.}}"
    {{/non_negotiables}}

  critical_paths:
    {{#critical_paths}}
    - "{{.}}"
    {{/critical_paths}}

  governance_profile: "{{governance_profile}}"
  operational_posture: "{{operational_posture}}"
