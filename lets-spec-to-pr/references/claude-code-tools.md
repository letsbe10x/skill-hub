# Claude Code Only

This skill uses the `Agent` tool to spawn subagents for parallel task decomposition.
This feature is only available in Claude Code. The skill cannot be ported to Cursor or Codex.

| Feature | Claude Code |
|---------|-------------|
| `Agent` | Spawn parallel subagents |
| `Skill` | Load and invoke skills |
